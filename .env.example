# Run scripts/generate-ca.sh to generate these values.

# Base64-encoded CA certificate (public, safe to share)
CA_CERT=

# Base64-encoded CA private key (keep secret — never commit this)
CA_KEY=

# Host port for the openclaw gateway
OPENCLAW_PORT=18789
